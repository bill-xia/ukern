USERINC := -Iinclude -Ilib

USERLIBS := lib/printf.o \
	lib/usyscall.o \
	lib/printfmt.o \

user/%: user/%.o $(USERLIBS)
	ld -T user/user.ld $^ -o $@

lib/%.o: lib/%.c
	$(CC) $(CFLAGS) $(USERINC) $< -c -o $@

user/%.o: user/%.c
	$(CC) $(CFLAGS) $(USERINC) $< -c -o $@
